[project]
name = "hellforge-mcp"
version = "0.1.0"
description = "MCP server for HellForge Doom 3 level editor"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
]

[project.scripts]
hellforge-mcp = "hellforge_mcp:run"
