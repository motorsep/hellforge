name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  linux:
    uses: ./.github/workflows/linux.yml

  windows:
    uses: ./.github/workflows/windows.yml

  appimage:
    uses: ./.github/workflows/appimage.yml

  flatpak:
    uses: ./.github/workflows/flatpak.yml

  release:
    name: Draft release
    needs: [linux, windows, appimage, flatpak]
    runs-on: ubuntu-latest

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Package artifacts
        run: |
          cd artifacts

          # Linux tarball
          tar czf hellforge-linux-x86_64.tar.gz -C hellforge-linux-x86_64 .

          # Windows zip
          cd hellforge-windows-x64
          zip -r ../hellforge-windows-x64.zip .
          cd ..

          # AppImage is already a single file
          cp hellforge-appimage-x86_64/*.AppImage .

          # Flatpak is already a single file
          cp hellforge-flatpak-x86_64/hellforge.flatpak .

      - name: Create draft release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          generate_release_notes: true
          files: |
            artifacts/hellforge-linux-x86_64.tar.gz
            artifacts/hellforge-windows-x64.zip
            artifacts/*.AppImage
            artifacts/hellforge.flatpak
